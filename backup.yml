backup:
  build: ./backup
  volumes_from:
    - dbdata:ro
  entrypoint:
    - python3
    - /opt/backup/backup.py

dbdata:
  image: ubuntu
  volumes:
    - /data/db
  command: /usr/bin/tail -f /dev/null
